/**
 * Tokens de color para APIs JS (p. ej. SweetAlert2) que no leen CSS variables.
 * Mantener alineado con $color-primary / --mc-primary en styles/base/_variables.scss.
 */
export const temaUi = {
  COLOR_PRIMARIO: '#8b0000',
} as const;
